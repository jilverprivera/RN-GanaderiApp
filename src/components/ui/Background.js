import React from 'react';
import {View} from 'react-native';
import {UIStyle} from '../../styles';

const Background = () => {
  return <View style={UIStyle.background} />;
};

export default Background;
