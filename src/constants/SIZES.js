import {Dimensions} from 'react-native';
const {width, height} = Dimensions.get('window');

export const SIZES = {
  // APP DIMESIONS
  width,
  height,
  // SIZES
  extraLarge: 50,
  large: 30,
  medium: 20,
  semiRegular: 18,
  regular: 16,
  small: 14,
  extraSmall: 12,
};
